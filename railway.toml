[build]
# Build command otomatis pakai requirements.txt
command = "pip install -r requirements.txt"

[deploy]
# Start command bot
start = "python bot.py"

[env]
# Token bot aman, jangan push token asli ke GitHub
BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"
ADMIN_ID = "123456789"
